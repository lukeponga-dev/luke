
import AdminLayout from '@/components/portfolio/admin-page-component';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <AdminLayout>{children}</AdminLayout>;
}
